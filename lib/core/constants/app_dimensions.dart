/// =============================================================================
/// AppDimensions - أبعاد التطبيق
/// =============================================================================
/// أبعاد ثابتة للمواصفات والأزرار والبطاقات والعناصر
/// =============================================================================

class AppDimensions {
  AppDimensions._();

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد الأزرار
  // ═══════════════════════════════════════════════════════════════════════════

  /// ارتفاع زر أساسي
  static const double buttonHeight = 56.0;

  /// ارتفاع زر صغير
  static const double buttonHeightSmall = 40.0;

  /// ارتفاع زر كبير
  static const double buttonHeightLarge = 64.0;

  /// نصف قطر حواف الأزرار
  static const double buttonRadius = 12.0;

  /// نصف قطر حواف الأزرار الصغيرة
  static const double buttonRadiusSmall = 8.0;

  /// نصف قطر حواف الأزرار الكبيرة
  static const double buttonRadiusLarge = 16.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد البطاقات
  // ═══════════════════════════════════════════════════════════════════════════

  /// ارتفاع بطاقة أساسي
  static const double cardHeight = 120.0;

  /// ارتفاع بطاقة صغير
  static const double cardHeightSmall = 80.0;

  /// ارتفاع بطاقة كبير
  static const double cardHeightLarge = 160.0;

  /// ارتفاع بطاقة الآية
  static const double verseCardHeight = 100.0;

  /// ارتفاع بطاقة الجملة
  static const double abjadCardHeight = 140.0;

  /// نصف قطر حواف البطاقات
  static const double cardRadius = 16.0;

  /// نصف قطر حواف البطاقات الصغيرة
  static const double cardRadiusSmall = 12.0;

  /// نصف قطر حواف البطاقات الكبيرة
  static const double cardRadiusLarge = 24.0;

  /// المسافة البادئة للبطاقة
  static const double cardPadding = 16.0;

  /// المسافة البادئة للبطاقة الصغيرة
  static const double cardPaddingSmall = 12.0;

  /// المسافة البادئة للبطاقة الكبيرة
  static const double cardPaddingLarge = 24.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد الأيقونات
  // ═══════════════════════════════════════════════════════════════════════════

  /// حجم أيقونة كبير
  static const double iconSizeLarge = 48.0;

  /// حجم أيقونة متوسط
  static const double iconSizeMedium = 32.0;

  /// حجم أيقونة صغير
  static const double iconSizeSmall = 24.0;

  /// حجم أيقونة مصغر
  static const double iconSizeMini = 16.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد حقول الإدخال
  // ═══════════════════════════════════════════════════════════════════════════

  /// ارتفاع حقل الإدخال
  static const double inputHeight = 56.0;

  /// ارتفاع حقل الإدخال الصغير
  static const double inputHeightSmall = 44.0;

  /// ارتفاع حقل الإدخال الكبير
  static const double inputHeightLarge = 72.0;

  /// نصف قطر حواف حقل الإدخال
  static const double inputRadius = 12.0;

  /// المسافة البادئة لحقل الإدخال
  static const double inputPadding = 16.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد المسافات
  // ═══════════════════════════════════════════════════════════════════════════

  /// المسافة الصغيرة
  static const double spacingXSmall = 4.0;

  /// المسافة الصغيرة
  static const double spacingSmall = 8.0;

  /// المسافة المتوسطة
  static const double spacingMedium = 16.0;

  /// المسافة الكبيرة
  static const double spacingLarge = 24.0;

  /// المسافة الكبيرة جداً
  static const double spacingXLarge = 32.0;

  /// المسافة الكبيرة جداً جداً
  static const double spacingXXLarge = 48.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد الهوامش
  // ═══════════════════════════════════════════════════════════════════════════

  /// الهامش الخارجي
  static const double marginSmall = 8.0;
  static const double marginMedium = 16.0;
  static const double marginLarge = 24.0;
  static const double marginXLarge = 32.0;

  /// الهامش الداخلي
  static const double paddingSmall = 8.0;
  static const double paddingMedium = 16.0;
  static const double paddingLarge = 24.0;
  static const double paddingXLarge = 32.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد القوائم
  // ═══════════════════════════════════════════════════════════════════════════

  /// ارتفاع عنصر القائمة
  static const double listItemHeight = 72.0;

  /// ارتفاع عنصر القائمة الصغير
  static const double listItemHeightSmall = 56.0;

  /// ارتفاع عنصر القائمة الكبير
  static const double listItemHeightLarge = 88.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد شريط التنقل
  // ═══════════════════════════════════════════════════════════════════════════

  /// ارتفاع شريط التنقل السفلي
  static const double bottomNavHeight = 80.0;

  /// ارتفاع شريط التطبيق
  static const double appBarHeight = 56.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد الأرقام
  // ═══════════════════════════════════════════════════════════════════════════

  /// حجم رقم النتيجة الكبير
  static const double resultNumberSize = 64.0;

  /// حجم رقم النتيجة المتوسط
  static const double resultNumberSizeMedium = 48.0;

  /// حجم رقم النتيجة الصغير
  static const double resultNumberSizeSmall = 32.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد الأيقونات الدائرية
  // ═══════════════════════════════════════════════════════════════════════════

  /// قطر أيقونة دائرية كبير
  static const double circularIconSizeLarge = 72.0;

  /// قطر أيقونة دائرية متوسط
  static const double circularIconSizeMedium = 56.0;

  /// قطر أيقونة دائرية صغير
  static const double circularIconSizeSmall = 40.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد الصور الرمزية
  // ═══════════════════════════════════════════════════════════════════════════

  /// حجم الصورة الرمزية كبير
  static const double avatarSizeLarge = 64.0;

  /// حجم الصورة الرمزية متوسط
  static const double avatarSizeMedium = 48.0;

  /// حجم الصورة الرمزية صغير
  static const double avatarSizeSmall = 32.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد الفقاعات
  // ═══════════════════════════════════════════════════════════════════════════

  /// قطر فقاعة محادثة المستخدم
  static const double chatBubbleRadius = 20.0;

  /// قطر فقاعة محادثة المستلم
  static const double chatBubbleRadiusAlt = 20.0;

  /// أقصى عرض لفقاعة المحادثة
  static const double chatBubbleMaxWidth = 280.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد الأرقام التسلسلية
  // ═══════════════════════════════════════════════════════════════════════════

  /// رقم السورة
  static const double surahNumberSize = 40.0;

  /// رقم الآية
  static const double ayahNumberSize = 24.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد شريط الطاقة
  // ═══════════════════════════════════════════════════════════════════════════

  /// ارتفاع شريط الطاقة
  static const double energyBarHeight = 8.0;

  /// نصف قطر شريط الطاقة
  static const double energyBarRadius = 4.0;

  // ═══════════════════════════════════════════════════════════════════════════
  // أبعاد الأقراص والرسوم البيانية
  // ═══════════════════════════════════════════════════════════════════════════

  /// قطر القرص الأساسي
  static const double wheelSize = 200.0;

  /// قطر القرص الصغير
  static const double wheelSizeSmall = 150.0;

  /// قطر القرص الكبير
  static const double wheelSizeLarge = 280.0;

  /// سمك الحلقة
  static const double wheelThickness = 20.0;

  /// سمك الحلقة الصغيرة
  static const double wheelThicknessSmall = 15.0;

  /// سمك الحلقة الكبيرة
  static const double wheelThicknessLarge = 30.0;
}